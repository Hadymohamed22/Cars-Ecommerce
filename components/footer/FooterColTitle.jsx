const FooterColTitle = ({ title }) => {
  return (
    <h5 className="capitalize font-semibold text-lg md:text-xl text-secondary mb-3">
      {title}
    </h5>
  );
};

export default FooterColTitle;
